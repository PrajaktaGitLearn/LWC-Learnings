import { createElement } from 'lwc';
import FifteenCom_ChildToParentComm_Child from 'c/fifteenCom_ChildToParentComm_Child';

describe('c-fifteen-com-child-to-parent-comm-child', () => {
    afterEach(() => {
        // The jsdom instance is shared across test cases in a single file so reset the DOM
        while (document.body.firstChild) {
            document.body.removeChild(document.body.firstChild);
        }
    });

    it('TODO: test case generated by CLI command, please fill in test logic', () => {
        // Arrange
        const element = createElement('c-fifteen-com-child-to-parent-comm-child', {
            is: FifteenCom_ChildToParentComm_Child
        });

        // Act
        document.body.appendChild(element);

        // Assert
        // const div = element.shadowRoot.querySelector('div');
        expect(1).toBe(1);
    });
});